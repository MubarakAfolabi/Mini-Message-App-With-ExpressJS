<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Message App</title>
  </head>
  <body>
    <h1><%= title %></h1>

    <ul>
      <% messages.forEach(message => { %>
      <li>
        <p><%= message.text %></p>
        <h3><%= message.user %></h3>
        <p><%= message.added %></p>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
